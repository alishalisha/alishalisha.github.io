<!doctype html>
<html lang="en-US" class="no-js">
  <head>
    <meta charset="utf-8">

    <meta name="google" value="notranslate" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge,chrome=1">
    <meta http-equiv="Content-Type" content="text/html; charset=utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1, user-scalable=yes">

    <!-- Use title if it's in the page YAML frontmatter -->
    <title><%= current_page.data.title %></title>

    <script type="text/javascript" src="http://fonts.sbnation.com/wvq7oai.js"></script>
    <link href='http://fonts.googleapis.com/css?family=Montserrat' rel='stylesheet' type='text/css'>
    <script type="text/javascript">try{Typekit.load();}catch(e){}</script>
    <% ooyala_player_code = 'b40f5b65292c4a6f8b3687c8670463fc' %>
    <script type="text/javascript" src="http://player.ooyala.com/v3/<%= ooyala_player_code %>?platform=html5-priority"></script>

    <%= stylesheet_link_tag "style" %>
    <%# This is  is how we pass data from config.rb, or ruby in general, to the javascript %>
    <script type="text/javascript">
      var App = App || {};
      App.Context = {
         url_prefix: "<%= url_prefix %>",
         app_name : "<%= app_name %>",
         host_name: "<%= url_host %>",
         twitter_name: "<%= twitter %>"
       };

      // A nice place holder for google analytics things
      var _gaq = _gaq || [];
    </script>
  </head>

  <body id="scrollbox">
    <%= partial "svg/svg" %>

   <nav>
      <span class="nav__vertical">
        <a class="nav__burger" href="#">☰</a>
        <a href="/">
          <svg class="nav__vertical__logo svg-icon" viewBox="0 0 474 86"><use xlink:href="#verge"></use></svg>
        </a>
      </span>
      <%= partial 'partials/hamburger_nav', :locals => {:weeks => weeks} %>
      <%= partial 'partials/social_share' %>
      <span class="nav__label">From our sponsor</span>
    </nav>
    <div class="nav__background"></div>
    <div class="nav__spacer"></div>

    <%= yield %>

  </body>
  <%= partial 'partials/analytics' %>
  <%= javascript_include_tag  "base" %>
</html>